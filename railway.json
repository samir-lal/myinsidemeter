{
  "build": {
    "commands": ["npm install", "npm run build", "mkdir -p server/public && cp -r dist/public/* server/public/"]
  },
  "start": {
    "command": "npm start"
  },
  "env": {
    "NODE_ENV": "production"
  }
}
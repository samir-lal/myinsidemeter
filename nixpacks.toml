# Railway deployment: 2025-07-22 15:30 - Fix 404 routing issue
[phases.build]
cmds = ["npm install", "npm run build"]

[phases.start]
cmd = "NODE_ENV=production node dist/index.js"

[variables]
NODE_ENV = "production"
NPM_CONFIG_PRODUCTION = "false"